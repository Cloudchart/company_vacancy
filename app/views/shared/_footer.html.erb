<%

links = []

if user_authenticated?
  links << {
    name: 'About us',
    href: main_app.company_path(ENV['ABOUT_US_COMPANY_ID'])
  }
end

links << {
  name: 'Support',
  href: "mailto:#{ENV['SUPPORT_EMAIL']}"
}

links << {
  name: 'Contacts',
  href: "mailto:#{ENV['REPLY_TO_EMAIL']}"
}

links << {
  name: 'Legal',
  href: main_app.page_path(:legal)
}

links << {
  name: 'Privacy',
  href: main_app.page_path(:privacy)
}

links << {
  name: 'Report content',
  href: '',
  id: 'report-content'
}

%>

<%= content_tag :footer, nil, data: {
  "react-class" => "components/footer_app",
  "react-props" => {
    links: links
  }
} %>
