<!DOCTYPE html>
<html>
  <head>
    <title>CloudChart</title>

    <%= favicon_link_tag "favicon_#{Rails.env}.ico" %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= render 'shared/meta' %>
  </head>

  <body>
    <header class="<%= yield(:class_for_header) %>">
      <%= render 'shared/header' %>
    </header>

    <%= render 'shared/flash' %>

    <main class="<%= yield(:class_for_main) %>">
      <%= yield %>
    </main>

    <footer class="<%= yield(:class_for_footer) %>">
      <%= render 'shared/footer' %>
    </footer>

    <div data-react-class="components/modal_stack"></div>

    <%= javascript_include_tag 'application' %>
    <%= pagescript %>
    <%= render 'shared/google_analytics' if Rails.env.production? %>
  </body>

</html>
