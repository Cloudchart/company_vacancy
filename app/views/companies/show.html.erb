<% title @company.name %>

<% set_meta_tags_for_social_networks(
  title: decorate(@company).og_name,
  description: t('meta.company.description', name: @company.name),
  image: @company.preview.try(:url),
  url: company_url(@company)
) %>

<% content_for :header_breadcrumbs do %>
  <%= render 'shared/companies/header_breadcrumbs' %>
<% end %>

<% content_for :header_menu do %>
  <%= render 'shared/companies/header_menu' %>
<% end %>

<% content_for :class_for_main, 'company' %>

<% content_for :class_for_body, 'company-body' %>
