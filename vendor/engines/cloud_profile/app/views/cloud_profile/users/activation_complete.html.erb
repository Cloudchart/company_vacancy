<%- class_for_root_tag main: 'cloud-profile' -%>

<article class="user" data-react-root="on">
</article>

<%-
  pagescript_params(
    user: CloudProfile::UserSerializer.new(current_user).as_json(root: false)
  )
-%>
